import styled from "styled-components";

const PostArea = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: .5rem;
`

const ProductName = styled.span`
  font-weight: bold;
  font-size: 22px;
`

export { PostArea, ProductName }